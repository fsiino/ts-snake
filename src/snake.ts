// type coords = [number, number];

export class Snake {
  private ctx : CanvasRenderingContext2D;
  private x: number;
  private y: number;
  
  private head: number[];
  
  private body: [number, number];
}